#!/bin/sh
# Strip Cursor co-author lines (install: git config core.hooksPath .githooks)
sed -i.bak '/^Co-authored-by: Cursor/d' "$1" 2>/dev/null || sed -i '' '/^Co-authored-by: Cursor/d' "$1"
rm -f "$1.bak"
